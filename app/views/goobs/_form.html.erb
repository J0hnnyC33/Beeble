<%= form_for(@goob) do |f| %>
  <% if @goob.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goob.errors.count, "error") %> prohibited this goob from being saved:</h2>

      <ul>
      <% @goob.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
